'use strict';

/**
 * meeting-recording controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::meeting-recording.meeting-recording');
